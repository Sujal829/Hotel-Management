bPORT=5000
MONGODB_URI=mongodb://localhost:27017/digital-diner
JWT_SECRET=change_me_in_production

# Used for stricter CORS in production (dev allows localhost:anyPort)
FRONTEND_URL=http://localhost:5173

# Dev-only OTP override (QA uses 0000)
DEV_OTP=0000

# Optional (only needed if you later switch from local uploads to Cloudinary)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

